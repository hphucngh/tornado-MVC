from . import BaseController
from datetime import date

class VersionHandler(BaseController):
    def get(self):
        response = { 'version': '3.5.1',
                     'last_build':  date.today().isoformat() }
        self.write(response)